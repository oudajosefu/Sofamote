{
  "name": "remote-media-control",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run build && npm start -w server",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client"
  }
}
